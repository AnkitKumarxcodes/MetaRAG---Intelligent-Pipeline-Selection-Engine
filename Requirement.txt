# ─────────────────────────────────────────
# MetaRAG — Intelligent Pipeline Selection Engine
# requirements.txt
# ─────────────────────────────────────────

# ── Document Loading ──────────────────────
pypdf==4.3.1                  # PDF reading
beautifulsoup4==4.12.3        # HTML + web page parsing
python-docx==1.1.2            # .docx Word file reading
pandas==2.2.2                 # CSV loading
requests==2.32.3              # URL + web page fetching
lxml==5.2.2                   # XML parser for sitemaps

# ── Chunking ──────────────────────────────
nltk==3.8.1                   # Sentence tokenisation
langchain==0.2.16             # RecursiveCharacterTextSplitter + core chains
langchain-text-splitters==0.2.4
langchain-community==0.2.16   # Chroma + document loader integrations
langchain-experimental==0.0.64 # SemanticChunker

# ── Embeddings (free, local) ──────────────
sentence-transformers==3.0.1  # Local embeddings, no API key needed
langchain-huggingface==0.0.3  # LangChain wrapper for HuggingFace embeddings

# ── Retrieval ─────────────────────────────
rank-bm25==0.2.2              # BM25 sparse retrieval
chromadb==0.5.5               # Local vector store for dense retrieval

# ── Evaluation ────────────────────────────
ragas==0.1.21                 # Faithfulness, relevancy, context precision
datasets==2.21.0              # HuggingFace datasets — required by ragas

# ─────────────────────────────────────────
# OPTIONAL — uncomment what you need
# ─────────────────────────────────────────

# ── Ollama (free local LLM + embeddings) ──
langchain-ollama==0.1.3
# Run: ollama pull llama3
# Run: ollama pull nomic-embed-text

# ── Groq (free tier API — fast inference) ─
langchain-groq==0.1.9

# ── OpenAI (paid) ─────────────────────────
# langchain-openai==0.1.23